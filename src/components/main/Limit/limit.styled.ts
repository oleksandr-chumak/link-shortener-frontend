import styled from 'styled-components'

export const LimitWrapper = styled.h4`
  color: ${({theme}) => theme.color.lite};
  font-size: 14px;
  font-weight: 200;
  margin-top: 10px;
`
export const LimitHighlight = styled.span`
  color: ${({theme}) => theme.color.brandPrimaryPink};
  font-weight: bold;
`