import styled from 'styled-components'

export const ClipboardCheckboxWrapper = styled.div`
  display: flex;
  gap: 10px;
  align-items: center;
`
export const ClipboardCheckboxHeader = styled.h4`
  font-size: 14px;
  font-weight: 300;
  color: ${({theme}) => theme.color.lite};
`